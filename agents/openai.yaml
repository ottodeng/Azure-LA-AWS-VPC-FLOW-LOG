interface:
  display_name: "Azure AWS VPC Flow Logs"
  short_description: "Analyze AWSVPCFlow through an enterprise read-only MCP service."
  default_prompt: "Run onboarding first, then analyze AWSVPCFlow through the secured MCP tools."
