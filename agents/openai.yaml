interface:
  display_name: "Azure AWS VPC Flow Logs"
  short_description: "Query AWSVPCFlow in Azure Log Analytics with safe read-only KQL."
  default_prompt: "Analyze AWS VPC Flow Logs in Azure Log Analytics using read-only KQL."
